load_pattern:
  - !linear
      to: 100%
      over: 10s
vars:
  port: ${e:PORT}
providers:
  short_provider: !range
    start: 0
    end: 1
endpoints:
- url: http://localhost:${v:port}/?id=${p:short_provider}
  peak_load: 20hps
