load_pattern:
  - linear:
      to: 100%
      over: 20s
vars:
  port: "${PORT}"
providers:
  short_provider:
    range:
      start: 0
      end: 2
endpoints:
  - url: http://localhost:${port}/?id=${short_provider}
    peak_load: 1hps
