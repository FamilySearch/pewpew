[package]
name = "channel"
version = "0.0.0"
edition = "2018"
publish = false
license = "Apache 2.0"

[lib]
doctest = false

[dependencies]
config = { path = "../config" }
crossbeam-queue = "0.2"
futures = "0.3"
serde = { version = "1.0", features = ["derive"] }