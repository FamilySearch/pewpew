[package]
name = "for_each_parallel"
version = "0.0.0"
edition = "2018"
publish = false

[lib]
doctest = false

[dependencies]
channel = { path = "../channel" }
config = { path = "../config" }
futures = "0.1"
tokio = "0.1"