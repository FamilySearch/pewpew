[package]
name = "for_each_parallel"
version = "0.0.0"
edition = "2018"
publish = false
license = "Apache 2.0"

[lib]
doctest = false

[dependencies]
channel = { path = "../channel" }
config = { path = "../config" }
futures = "0.3"
tokio = "0.2"

[dev-dependencies]
futures-timer = "3"