[package]
name = "ether"
version = "0.0.0"
edition = "2018"
publish = false

[lib]
doctest = false

[dependencies]
futures = "0.1"
tokio = { version = "0.1", optional = true }

[features]
async-write = ["tokio"]