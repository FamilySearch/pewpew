[package]
name = "channel"
version = "0.0.0"
edition = "2018"
publish = false

[lib]
doctest = false

[dependencies]
config = { path = "../config" }
crossbeam-queue = "0.2"
futures = "0.1"
serde = { version = "1.0", features = ["derive"] }
tokio = "0.1"